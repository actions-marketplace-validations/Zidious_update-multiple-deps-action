{
  "name": "update-multiple-deps-action",
  "version": "1.0.0",
  "description": "GHA for updating multiple dependencies",
  "main": "lib/index.js",
  "author": "Gabe Olesen",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "build:packaged": "yarn build && ncc build -o dist"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/exec": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.1.1",
    "@vercel/ncc": "^0.36.1",
    "prettier": "^2.8.8",
    "typescript": "^5.0.4"
  }
}
